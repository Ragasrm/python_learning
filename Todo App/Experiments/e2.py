#while True:
    #print("Hi")


# should not declear the variable inside the loop:
todos = []
while True:
    user_prompt = "Enter the todo:"
    todo = input(user_prompt)
    todos.append(todo)
    print(todos)

# capitalize  --> first lette of the setence
# Tittle --> first lette of each word on sentence
